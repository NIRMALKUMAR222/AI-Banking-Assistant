# ── Core API Framework ──────────────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.12      # FastAPI form/file upload support
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1          # .env loading in frontend api_client

# ── LLM (OpenAI-compatible: Groq & xAI Grok) ───────────────────────────────
openai==1.51.0

# ── Embeddings (local, no API key required) ─────────────────────────────────
sentence-transformers==3.1.1
torch==2.4.1
transformers==4.44.2

# ── Vector Stores ───────────────────────────────────────────────────────────
faiss-cpu==1.8.0              # Default vector store (fast, in-process)
chromadb==0.5.11              # Optional: set VECTOR_STORE_BACKEND=chroma

# ── RAG / Text Splitting ─────────────────────────────────────────────────────
langchain-text-splitters==0.3.0

# ── Synthetic Data & Utilities ───────────────────────────────────────────────
faker==30.3.0
numpy==1.26.4
httpx==0.27.2

# ── Frontend ─────────────────────────────────────────────────────────────────
streamlit==1.39.0
pandas==2.2.3

# ── Security ─────────────────────────────────────────────────────────────────
bcrypt==5.0.0                 # Password hashing (used directly, not via passlib)
python-jose[cryptography]==3.3.0  # JWT tokens